{
  "name": "vcag",
  "displayName": "VCAG - Coordinate Graph & Map Viewer",
  "description": "Just select messy text containing numbers — no formatting needed. Extracts coordinates and visualizes as 2D/3D graph (Plotly) or map (Leaflet + OpenStreetMap)",
  "version": "0.6.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/akihiro-suzuk1/vcag.git"
  },
  "icon": "images/icon.png",
  "license": "MIT",
  "publisher": "a-suzuki",
  "engines": {
    "vscode": "^1.96.0"
  },
  "categories": [
    "Visualization"
  ],
  "keywords": [
    "coordinate",
    "graph",
    "plot",
    "map",
    "visualization"
  ],
  "main": "./dist/extension.js",
  "activationEvents": [],
  "contributes": {
    "commands": [
      {
        "command": "coordGraph.showFromSelection",
        "title": "Coord Graph: Plot Coordinates from Selection"
      },
      {
        "command": "coordGraph.showFromClipboard",
        "title": "Coord Graph: Plot Coordinates from Clipboard"
      }
    ]
  },
  "scripts": {
    "compile": "npm run check-types && node esbuild.js",
    "check-types": "tsc --noEmit",
    "watch": "npm-run-all -p watch:*",
    "watch:esbuild": "node esbuild.js --watch",
    "watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
    "vscode:prepublish": "npm run package",
    "package": "npm run check-types && node esbuild.js --production",
    "build": "vsce package",
    "release": "bash scripts/release.sh",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/vscode": "^1.96.0",
    "@vscode/vsce": "^3.7.1",
    "esbuild": "^0.25.0",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.7.0",
    "vitest": "^4.0.18"
  },
  "dependencies": {
    "leaflet": "^1.9.4",
    "plotly.js-dist-min": "^3.4.0",
    "svgpath": "^2.6.0"
  }
}
