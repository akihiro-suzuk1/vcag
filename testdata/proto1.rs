// テスト用: 座標データを含むRustコード
// 選択範囲から座標を抽出するProto 1の動作確認に使用

fn main() {
    // 2D座標: 建物の外周
    let building_outline = vec![
        (35.6812,139.7671),
        (35.6815,139.7675),
        (35.6820,139.7670),
        (35.6817,139.7666),
        (35.6812,139.7671),
    ];

    // 3D座標: 飛行経路 (lat, lon, alt)
    let flight_path: Vec<(f64, f64, f64)> = vec![
        (35.5494,139.7798,0.0),
        (35.5500,139.7800,100.5),
        (35.5520,139.7810,500.0),
        (35.5600,139.7850,3000.0),
        (35.5800,139.7900,6000.0),
        (35.6000,139.8000,10000.0),
    ];

    // コメント中の座標
    // 原点: 0,0  中心: 50,50  終点: 100,100

    // CSV風のログデータ
    println!("x,y,pressure");
    let sensor_data = "
        10.0,20.0
        12.5,22.3
        15.0,25.1
        17.5,28.8
        20.0,33.2
        22.5,38.0
        25.0,42.5
    ";

    // 配列リテラル風
    let polygon = [[0,0], [10,0], [10,10], [0,10], [0,0]];

    // ノイズ混じり: 座標とそうでない数値が混在
    let config_port = 8080; // これは座標ではない
    let point_a = (1.0, 2.0); // これは座標
    let threshold = 0.95; // これも座標ではない
}
